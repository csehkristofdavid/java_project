/**
 * This is a Controller package, where the main functions
 * of the application are implemented.
 *
 * There are 3 classes which are represent the 3 windows of
 * the application.
 *
 * The PrimaryController implements the starting window.
 * It has 2 Buttons for the navigation.
 *
 * The GameController implements the game window.
 * It has functions for the game logic.
 *
 * The StatisticsController implements the stats window.
 * It has the table for the statistics. It puts the data
 * to the right column.
 */
package controll.controllers;
